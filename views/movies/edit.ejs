<!DOCTYPE html>
<html>
<head>
  <title>Edit <%= movie.title %></title>
</head>
<body>
  <h1>Edit <%= movie.title %></h1>
  <form action="/movies/<%= movie._id %>?_method=PUT" method="POST">
    <label for="title">Title:</label>
    <input type="text" name="title" id="title" value="<%= movie.title %>" required>
    <br>
    <label for="director">Director:</label>
    <input type="text" name="director" id="director" value="<%= movie.director %>" required>
    <br>
    <label for="releaseDate">Release Date:</label>
    <input type="date" name="releaseDate" id="releaseDate" value="<%= movie.releaseDate.toISOString().substring(0, 10) %>" required>
    <br>
    <label for="genre">Genre:</label>
    <input type="text" name="genre" id="genre" value="<%= movie.genre %>" required>
    <br>
    <button type="submit">Update Movie</button>
  </form>
  <a href="/movies/<%= movie._id %>">Cancel</a>
</body>
</html>
